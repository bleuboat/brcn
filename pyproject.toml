[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"

[project]
name = "brcn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "bleuboat", email = "3751541788@qq.com" }
]
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "playwright>=1.62.0",
    "regex>=2026.9.3",
    "typer>=0.27.2",
]

[project.scripts]
brcn = "brcn:app"

[dependency-groups]
dev = [
    "ruff>=0.16.5",
]
